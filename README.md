UIView-Frame
============

Category for UIView extends the frame accessibility 
___
**Usage:**
For example - instead of

    CGRect rect = myView.frame;
    rect.size.width += 100;
    myView.frame = rect;
you may use 

    myView.width+=100;

